# Contracts

`openapi.yaml` is the language-neutral HTTP contract. `src/index.ts` contains matching client-facing TypeScript shapes. Run:

```bash
apps/mobile/node_modules/.bin/tsc -p packages/contracts/tsconfig.json
```
