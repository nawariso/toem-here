/** Field guidance shown on the Scan screen (Requirement 003). */
export const GUIDANCE = ['Side of the head works best', 'Keep your distance', 'Don’t chase', 'Don’t feed'] as const;
export const MOTTO = 'Observe. Don’t Disturb.';
